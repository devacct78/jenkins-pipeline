pipeline {

    agent any

    stages {

        stage("hello") {
            steps {
                echo "hello world"
            }
        }

        stage("End") {
            steps {
                echo "end of pipeline"
            }
        }
    }
}